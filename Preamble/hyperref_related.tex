%!TEX root = ../main.tex
% vim: nospell

% \usepackage{footnotebackref} % Breaks footnotemark and footnotetext https://tex.stackexchange.com/questions/78423/how-to-use-the-footnotebackref-package-with-footnotemark-and-footnotetext
% \usepackage{hyperref}

\usepackage[\pdfxopts]{pdfx}
% \usepackage{hyperref} % comment if using the pdfx package
\hypersetup{%
    % bookmarks=true,
    % hyperfigures,
    % hyperindex = true,
    % pagebackref,
    % pdfauthor = {Krishnakumar Gopalakrishnan},
    % pdfpagelabels,
    % pdfpagelayout = OneColumn
    % pdfpagemode = UseOutlines,
    anchorcolor = black,
    bookmarksdepth = 4,
    bookmarksnumbered = true,
    bookmarksopen = true,
    bookmarksopenlevel = 0,
    breaklinks = true,
    citecolor = [RGB]{0,62,116}, % imperialblue
    colorlinks = true,
    final = true,
    hyperfootnotes=false,
    linkcolor = [RGB]{0,62,116}, % imperialblue
    linktocpage,
    pdfborderstyle={/S/U/W 1},
    pdfdisplaydoctitle = false,
    pdflang=Englisch,
    pdfmenubar=true,
    pdfstartview={Fit},
    pdftoolbar=true,
    plainpages = false,
    urlcolor  = [RGB]{0,62,116}, % imperialblue
}%

% \usepackage[ocgcolorlinks]{ocgx2} % viewer support is bad. Do not rely on this one.
\usepackage{nameref}

\usepackage{algorithm}% http://ctan.org/pkg/algorithms

\usepackage[all]{hypcap} % to be loaded after hyperref. fix hyperref links to jump directly to Table or Figure

% \RequirePackage{calc}     % calculate vertical spacing
% \usepackage{pdftexcmds} % not required if using pdfx package (since it is already loaded by it)
% \usepackage[translate=babel,abbreviations,nomain,indexonlyfirst,acronym,toc,xindy,automake,hyperfirst=false]{glossaries-extra} % should be loaded after hyperref % consider 'symbols' package option

\usepackage[nomain,acronym,xindy]{glossaries-extra}% should be loaded after hyperref % consider 'symbols' package option

% \usepackage{minitoc}

% \usepackage{hypdestopt} % seems to have problems with pdfx package?
% Adding package bookmark improves bookmarks handling.
% More features and faster updated bookmarks.
\usepackage{bookmark}
\usepackage[nameinlink]{cleveref}

